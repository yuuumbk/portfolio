'use strict'; var $loading = $('.is-loading, .loading'), $content = $('.wrapper'); $loading.css({ display: 'block' }); $content.css({ display: 'none' }); $(function () { changeDisplay(); function changeDisplay() { $(window).trigger('scroll'); $content.css({ display: 'block' }); $loading.delay(500).fadeOut() } })